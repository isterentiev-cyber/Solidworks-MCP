# SolidWorks MCP Server Dependencies
# Upstream author: Samsaam Ali Baig
# Fork hardening 2026-09-01:
#   - versions pinned exactly (upstream used >=, unsafe against registry compromise)
#   - removed `asyncio-compat>=0.1.0`: package does not exist on PyPI (404).
#     Phantom dependency — broke install and left a slopsquatting hole
#     (anyone could register the name and own every user of this repo).

# MCP SDK — 1.x line: repo targets the low-level Server API, mcp 2.x is breaking
mcp==1.29.0

# COM automation (cp314 wheels available)
pywin32==312

# .env loading
python-dotenv==1.2.2
